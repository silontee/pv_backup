# Project: 태양광 변동성 대비 화력 백업 최적화 플랫폼 (한국남동발전)

## PRD Version Management
- PRD 파일은 `plan/` 디렉토리에 저장한다.
- 새로 PRD를 작성하거나 수정할 때마다 버전을 올려서 새 파일로 저장한다.
  - 형식: `plan/plan_v{N}` (예: plan_v1, plan_v2, plan_v3 ...)
- 현재 최신 버전: **v7** (`plan/main/plan_v7.md`)
- 문제 정의: `plan/main/problem_definition.md`
- v6부터는 `plan/main/` 디렉토리에서 관리
- 이전 버전(v1~v5)은 `plan/` 루트에 히스토리로 보존한다.

---

## 프로젝트 컨텍스트 요약

### 프로젝트 핵심 정의
- 한국남동발전의 **태양광 변동성 대비 시스템**
- 태양광은 변동비 0원 → 무조건 최대 발전, 전량 입찰이 원칙
- 문제는 **"내일 얼마나 나올지 모른다"** → 예측 오차 10~30%, 급변 시 50%+
- 화력은 이미 최소출력(~6,387MW)으로 24시간 가동 중 → 정확하지만 느림
- **태양광 예측을 잘 해서 화력에게 미리 정확한 지시를 내리는 것**이 핵심
- 이 지시 = D-1일 KPX 가용용량 신고 = 결과적으로 입찰 최적화

### 프레이밍 변천 (PRD 버전별)
- **v1**: "발전 스케줄링 및 의사결정 지원 플랫폼" (모호, 입찰 맥락 없음)
- **v2**: "D-1일 10시 가용용량 신고 최적화" (CBP 시장 구조 반영, 입찰 중심)
- **v3**: "태양광 변동성 대비 → 부가적으로 입찰 최적화" (변동성 대비 중심으로 전환)
- **v4**: v3 + 3단계 운영(사전/실시간감시/실시간대응) + 기상데이터 전략 + 화력 운전제약 구체화

### 핵심 결정사항 (확정)
1. **현행 CBP 시장 기준만** — 향후 시장 개편(입찰시장 전환 등)은 고려하지 않음
2. **하루전 시장(Day-ahead) 기준** — 10시/17시 2차 제출
3. **포트폴리오 기반** — 제도상 발전기별 개별 입찰이지만, 내부적으로 포트폴리오로 최적화
4. **SMP 예측은 우리가 안 함** — SMP 과거 데이터는 참조만
5. **변동성 대비가 핵심** — 입찰 최적화는 변동성 대비의 자연스러운 부산물
6. **사람이 최종 판단** — 시스템은 추천, 운영자가 확정 (자동 제어 X)

---

## 한국 전력시장 구조 (파악 완료)

### CBP (Cost-Based Pool) 구조
- 변동비(연료비) = 입찰가 (자유 입찰 아님)
- KPX가 Merit Order 결정, SMP(마지막 급전 발전기 변동비)로 전원 정산
- 발전사가 할 수 있는 것: **가용용량 신고** (어떤 발전기를 켤지, 얼마나 낼 수 있는지)

### 입찰 타임라인
- D-1일 10:00 → 1차 예측 & 가용용량 신고
- D-1일 17:00 → 2차 보정
- D-1일 저녁 → KPX 급전계획 확정
- D일 → 실시간 급전

### 신재생에너지 예측제도 (2021.10 시행)
- 20MW 초과 태양광/풍력 → D-1일 10시/17시 예측 제출 의무
- 기준오차율 충족 시 인센티브, 미충족 시 페널티
- **남동발전도 이미 예측 제출 중** (구체 시스템 미공개)
- 10시/17시는 **KPX 전력시장 스케줄** (기상청 기준 아님)

### 계통 제약
- 필수운전(Must-run): KPX가 계통 안정성 위해 강제 가동 지정
- 최소출력: 석탄 ~60%, LNG ~48% (이하로 못 내림)
- 예비력 확보 의무
- 열병합(CHP) 열수요 의무

---

## 남동발전 포트폴리오 (파악 완료)

### 화력
| 발전소 | 용량(MW) | 발전원 | 최소출력(추정) |
|--------|---------|--------|-------------|
| 영흥 1~6호기 | 5,080 | 석탄 | ~3,048MW (60%) |
| 삼천포(고성) 3~6호기 | 2,080 | 석탄 | ~1,248MW (60%) |
| 강릉(안인) | 2,080 | 석탄 | ~1,248MW (60%) |
| 분당 | 920 | LNG 복합 | ~442MW (48%) |
| 여수 | 669 | 석탄/중유 | ~401MW (60%) |
| 영동 | - | 나무펠릿(바이오매스) | - |
| **합계** | **~10,829** | | **~6,387** |

### 신재생
- 태양광: 20+ 사이트 (영흥, 삼천포, 수상, 영농형 등)
- 풍력: 영흥, 군위 등
- 연료전지: 여수, 분당, 안산
- 해양소수력: 삼천포, 영흥

### 3영역 구조
```
전체 공급 = 고정 영역(~6,387MW) + 예측 영역(태양광+풍력 변동) + 최적화 영역(0~4,442MW)
```

---

## 데이터 현황

### 확보 완료 (CSV)
- 발전소 운영 현황 (에너지원/설비용량)
- 전력거래 입찰 정보 현황 (시간대별 SMP 결정 발전기)
- SMP 수요예측 정보 (시간대별 SMP 가격)
- 발전소 호기별 작업현황
- 신재생에너지 사업현황 (사이트별 위치/용량)
- 설비 운영 현황 (탈질설비 등)
- 여수발전 연료소비실적
- 온배수 배출량

### 크롤링 필요 (남동발전 홈페이지 링크데이터)
- **시간대별 화력 발전실적** ← 핵심
- **시간대별 태양광 발전실적** ← 핵심
- **시간대별 풍력 발전실적**
- **시간대별 연료전지 발전실적**
- **일자별 발전소 기상정보** ← 핵심
- 대기오염물질 배출실적
- 발전소 주변 농도

### 크롤링 구조 파악 완료
- 남동발전 홈페이지: `getPowerList.do` (발전소 목록), `csvDown.do` (CSV 다운로드)
- 발전소 39개 사이트 목록 확인 (화력 5개 + 태양광 20+ + 풍력/연료전지/소수력)
- 날짜 입력 필드가 readonly → **Playwright 필요**
- `crawl_koen.py` 스크립트 작성 완료 (미실행)

### API
- 공공데이터포털 API 키 발급 완료 (`.env`에 저장)
- 단, 각 API별 **활용 신청이 별도로 필요** (현재 403 반환)

---

## 태양광 예측 관련 파악 사항

### 예측 오차 현실
| 날씨 | 오차 수준 | 빈도 |
|------|----------|------|
| 맑은 날 | 2~5% | 30~40% |
| 구름 조금 (Partly Cloudy) | 15~30% | 30~40% |
| 흐린 날 | 20~40% | 15~20% |
| 급변 (Ramp Event) | 30~60% | 5~10% |

- **핵심: 평균 오차가 아니라 최악의 날 오차가 문제**
- 오후 14~17시가 가장 위험 (구름 발생 + 일몰 접근 + Duck Curve)

### 벤치마크
- 한국중부발전: AI 예측 정확도 99% (2026.2), 2030년까지 신재생+화력 통합 AI 목표
- 한전: AI 재생에너지 예측 기술 개발 완료 (2021), 2km 격자
- 남동발전: 미공개 → **우리 플랫폼의 기회**

### 기상 데이터 전략
- "기상청이 틀린만큼 틀린다" → **다중 소스로 극복**
  - LDAPS (하루 8회) → 하루전 예측 주입력
  - 초단기예보 (30분마다) → 실시간 보정
  - 위성 영상 (10분마다) → 구름 급변 탐지 (NWP보다 초단기에서 우수)
  - 발전소 현장 실측 → 피드백 보정
  - 과거 패턴 학습 → 기상청 오차 보정

### PV 예측 모델 방향
- **MVP**: 검증된 ML (XGBoost 또는 LSTM)로 시작
- **장기**: 베이지안 계층모형 + FiLM-조건부 GRU decoder-only (pv_predict.md 참조)
- **전제**: 남동발전 기존 예측보다 정확해야 가치 있음
- 모델 수식 세부사항은 구현 단계에서 다듬기로 함

---

## 화력 운전 제약 파라미터 (업계 표준 추정)

| 파라미터 | 영흥(석탄) | 삼천포(석탄) | 분당(LNG) |
|---------|----------|-----------|----------|
| Pmax | 870MW/호기 | 560MW/호기 | 460MW |
| Pmin | 520MW (60%) | 336MW (60%) | 221MW (48%) |
| 램프업 | ~15MW/분 | ~10MW/분 | ~30MW/분 |
| 최소기동시간 | 12시간 | 12시간 | 4시간 |
| 최소정지시간 | 8시간 | 8시간 | 2시간 |
| Cold 기동 | 16시간 | 16시간 | 6시간 |
| 기동비용 | ~3억원/회 | ~2억원/회 | ~5천만원/회 |
| 변동비 | ~80원/kWh | ~80원/kWh | ~160원/kWh |

**핵심 trade-off**: 석탄 = 싸지만 느리다 vs LNG = 비싸지만 빠르다

---

## 플랫폼 3단계 운영 모드 (v4 확정)

1. **사전 대비 (D-1일)**: 하루전 예측 + 화력 대응 계획 + KPX 신고
2. **실시간 감시 (D일)**: 예측 vs 실측 괴리 감지, 3~6시간 예측 갱신
3. **실시간 대응 (이벤트)**: 액션 플랜 즉시 업데이트, 화력 조정 지시

---

## 아직 구체화 필요한 핵심 항목

| # | 항목 | 상태 |
|---|------|------|
| 1 | **PV 예측 모델 구체 설계** (입력→모델→출력 파이프라인) | 개념 있음, 구체화 필요 |
| 2 | **PV 예측 학습/검증 방법** (데이터 분할, 평가 지표) | 미정 |
| 3 | **화력 상태 모델링** (발전기별 현재 상태 파악/입력 방법) | 미정 |
| 4 | **수요 예측/목표 공급량 설정** | 미정 |
| 5 | **PV→화력 연결 로직 (UC) 구체 구현** | 수식 있음, 구현 설계 필요 |
| 6 | **실시간 피드백 루프** | 개념만 있음 |
| 7 | **기술 스택/아키텍처** (DB, 프론트, 백엔드) | 미정 |
| 8 | **대시보드 UI/UX 설계** | 워크플로우 정의됨, UI 미정 |
| 9 | **대기질 확산 모델** (가우시안 플룸 등) | 개념만 있음 |

→ 다음 단계: **1번(PV 모델)과 3번(화력 상태 모델링)을 같이 잡는 것**이 우선

---

## 파일 구조
```
D:\pv_backup\
├── CLAUDE.md              ← 이 파일 (프로젝트 컨텍스트)
├── .env                   ← 공공데이터포털 API 키
├── references.md          ← 참고자료/출처 목록
├── plan/
│   ├── plan_v1~v4         ← 총괄 PRD 이전 버전들
│   ├── plan_v5            ← 총괄 PRD (최신)
│   ├── pv/
│   │   └── v1             ← PV 예측 세부 계획 (논문 모델 기준)
│   ├── fuel/              ← 화력 백업 최적화 세부 계획 (예정)
│   └── esg/               ← 대기질 영향 세부 계획 (예정)
├── pv/                    ← ★ PV 예측 연구 전용 디렉토리
│   ├── README.md          ← PV 연구 개요
│   ├── plan_v1.md         ← PV 예측 계획 v1 (논문 모델 기준)
│   ├── pv_predict.md      ← 모델 논문 (베이지안+FiLM-GRU 수식)
│   ├── notebooks/         ← EDA, 실험 노트북
│   ├── src/
│   │   ├── data/          ← 데이터 로딩, 전처리, 피처 엔지니어링
│   │   ├── models/        ← 모델 정의
│   │   ├── training/      ← 학습/검증/평가 파이프라인
│   │   └── utils/         ← 공통 유틸리티
│   └── experiments/       ← 실험 결과, 모델 체크포인트
├── data/
│   └── raw/               ← 크롤링 데이터 저장 (미수집)
├── data_fetch.py          ← 공공데이터 API 호출 (API 활용신청 필요)
├── crawl_koen.py          ← 남동발전 홈페이지 Playwright 크롤링 (미실행)
├── 한국남동발전㈜_발전소 운영 현황_20250728.csv
├── 한국남동발전㈜_전력거래 입찰 정보 현황_20241231.csv
├── 한국남동발전㈜_SMP 수요예측 정보_20241201.csv
├── 한국남동발전㈜_발전소 호기별 작업현황_20221231.csv
├── 한국남동발전㈜_(발전공기업 표준) 신재생에너지 사업현황_20221231.csv
├── 한국남동발전㈜_설비 운영 현황_20151231.csv
├── 한국남동발전㈜_여수발전 연료소비실적_현황_20171231.csv
└── 한국남동발전㈜_온배수 배출량_20241231.csv
```
